# diogomendespost-meta

Como add sub modules:
  git submodule add <URL_do_repositório_do_submódulo> [caminho_local]

Git pull em projetos com submodules.
  git clone --recursive <URL_do_repositório_principal>
